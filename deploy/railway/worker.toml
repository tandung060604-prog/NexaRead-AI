[build]
builder = "DOCKERFILE"
dockerfilePath = "backend/Dockerfile"

[deploy]
startCommand = "dramatiq app.tasks.document_processing --processes 1 --threads 1"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
